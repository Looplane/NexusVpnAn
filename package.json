{
    "name": "nexusvpn-root",
    "private": true,
    "scripts": {
        "start": "powershell ./start-all.ps1",
        "install:all": "npm install --prefix frontend && npm install --prefix backend && npm install --prefix mobile"
    }
}